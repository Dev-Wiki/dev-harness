# 活跃任务索引

> [!NOTE]
> 本文档是 [Dashboard.md](Dashboard.md) 的执行入口，只保存当前工作顺序、依赖、共享验证基线和任务文件链接。完整任务正文分别维护在 `tasks/<Task-ID>.md`，完成后迁入 `archive/<milestone>/`。

## 1. 当前工作顺序

1. [{任务编号} — {任务名称}](tasks/{任务编号}.md)：{为什么先做或当前下一步}
2. [{任务编号} — {任务名称}](tasks/{任务编号}.md)：{依赖关系或起手条件}

## 2. 活跃任务索引

| 任务编号 | 任务 | 状态 | 依赖 | 权威详情 |
|---|---|---|---|---|
| {任务编号} | {任务名称} | 📋 规划中 | {依赖任务或外部条件} | [tasks/{任务编号}.md](tasks/{任务编号}.md) |

## 3. 阻塞与决策

| 任务编号 | 阻塞 / 待确认项 | 负责人 / 解除条件 |
|---|---|---|
| {任务编号} | {未知协议、接口、设备、账号或业务决策} | {负责人或证据要求} |

## 4. 共享验证基线

```bash
{适用于当前里程碑的构建或测试命令}
```

- {跨任务共同验收条件}
- 单任务专属验收和证据只写在对应任务文件中。

## 5. 生命周期规则

- 状态变化时同步更新本索引和 [Dashboard.md](Dashboard.md)。
- 完成任务记录最终验收证据后，迁入 `archive/{里程碑}/`，并从活跃顺序和索引移除。
- 不在本文件追加逐次命令输出、过程日志或已完成任务正文。

---

*最后更新：{YYYY-MM-DD}（{更新说明}）*
